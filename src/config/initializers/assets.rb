# frozen_string_literal: true

# Asset pipeline configuration.
# Rails 8 uses Propshaft (or dartsass-rails for SCSS) instead of Sprockets.
# config.assets.* options are no longer available.
#
# To add asset paths, use:
#   config.assets.paths << Rails.root.join('node_modules')
